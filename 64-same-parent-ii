public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
   return ((acc.Id != null) && (con.AccountId != null) && (opp.AccountId != null) && (con.AccountId == acc.Id) && (opp.AccountId == acc.Id)); 
}